# Codebase Review Ledger

This ledger records governed review findings for `lotus-report`.

Tracking model:

1. historical review evidence remains in this ledger for audit and closure context,
2. active validated backlog findings live in GitHub issues, not local-only ledger rows,
3. the current enterprise refactor discovery ledger is
   [#109](https://github.com/sgajbi/lotus-report/issues/109),
4. new local ledger rows for active work must link the corresponding GitHub issue and include the
   focused validation evidence used to move that issue to `status/fixed-local`, `status/pr-open`,
   or `status/merged-main`,
5. local review notes without a linked issue are historical evidence only unless an explicit issue
   is created or reused.

## RFC-0105 Gold-Pass Audit

Review id: `RFC-0105-GOLD-PASS-2026-04-29`

Scope: RFC-0105 reporting observability, operations, replay tooling, implementation proof, and
closure posture.

| Slice | Status | Findings | Actions taken | Evidence |
| --- | --- | --- | --- | --- |
| Slice 0 platform scaffold | Signed off | Platform-owned scaffold defaults are documented in the platform RFC and protected by platform tests. No report-local code debt found. | No report change needed. | Platform RFC proof ledger and `tests/unit/test_repository_hygiene_scaffold_contract.py`. |
| Slice 1 observability structure | Signed off | Runtime vocabulary is centralized in `src/app/observability.py`; no duplicate local field registry found in the reviewed RFC-0105 surface. | No code change needed. | `tests/unit/test_observability.py`; RFC proof ledger. |
| Slice 2 trace and structured logging | Signed off | Trace/correlation propagation is source-backed across report/render/archive. Sensitive log proof remains bounded to captured evidence, not a universal data-loss-prevention claim. | No code change needed. | Slice 2 closure evidence and RFC proof ledger. |
| Slice 3 metrics, dashboards, alerts, SLA contracts | Signed off | Metric vocabulary is bounded in `src/app/reporting_metrics.py`; platform dashboard and alert contracts intentionally reference implemented metrics only. | No code change needed. | Platform reporting observability contract tests and report observability tests. |
| Slice 4 diagnostics | Signed off | Diagnostics API is support-safe and intentionally one-job scoped. Broader lookup surfaces remain planned. | No code change needed. | `tests/integration/test_report_job_api.py`; OpenAPI quality gate. |
| Slice 5 rerender | Hardened | Live proof exposed a render-contract mismatch that was already fixed. Audit found live proof captures kept only the latest render/archive request and response, which weakened per-operation inspection. | Added numbered render/archive request and response captures while preserving latest stable filenames. | `scripts/rfc_0102_proof_app.py`; live proof harness. |
| Slice 6 regenerate | Hardened | Regenerate behavior is implementation-backed and separate from rerender. Same capture-overwrite evidence weakness applied to regenerate proof inspection. | Same capture sequencing fix records regenerate render/archive calls independently. | `scripts/rfc_0102_proof_app.py`; live proof harness. |
| Slice 7 replay | Hardened | Failed job and batch-item replay have explicit eligibility and idempotency tests. Same capture-overwrite evidence weakness applied to replay-generated render/archive calls. | Same capture sequencing fix records replay render/archive calls independently. | `scripts/rfc_0102_proof_app.py`; replay unit/integration suites. |
| Slice 8 attention scanning | Signed off | Attention events are source-backed, bounded, and support-safe. Broader productized dashboards remain out of RFC-0105 scope. | No code change needed. | `tests/unit/reporting_operations/test_attention.py`; attention API integration test. |
| Slice 9 live proof | Hardened | Proof is end-to-end and meaningful, but evidence inspection was improved by preserving every render/archive call instead of overwriting captures. | Added per-call numbered evidence captures for render and archive clients. | `scripts/rfc_0105_live_evidence.py`; live evidence pack rerun required after this audit change. |
| Closure docs and supported features | Signed off | Final RFC clearly bounds first-wave completion versus RFC-0106/RFC-0107 residual scope. Audit requested an explicit debt-removed statement. | Platform RFC final assessment updated to call out slice-by-slice audit and debt removed. | Platform RFC and closure governance test. |

Follow-up:

1. Rerun the RFC-0105 live evidence harness after the capture sequencing change and record the new
   evidence directory in the platform RFC.
2. Keep the known published-wiki drift as a pre-existing publication issue unless this branch takes
   ownership of wiki publication cleanup.

## Enterprise Backend Refactor Issue Closure

| Issue | Status | Finding | Actions taken | Evidence | Docs/wiki/context decision |
| --- | --- | --- | --- | --- | --- |
| [#131](https://github.com/sgajbi/lotus-report/issues/131) | Hardened | Portfolio-review holdings consumed lotus-core position rows but dropped the governed `HoldingsAsOf:v1` source-product envelope, source quality/reconciliation posture, latest evidence timestamp, restatement/source-lineage fields, position-state status, maturity date, row snapshot/evidence identity, and row source identifiers. | Added source-product and supportability mapping for holdings, surfaced holdings metadata through `holdings`, key figures, client-section items, and evidence source refs, preserved source position-state and row identity fields where provided, and degraded supportability for partial, stale, unknown, reconciliation-incomplete, or trust-metadata-incomplete HoldingsAsOf responses. Same-pattern scan covered holdings mapper, key figures, section readiness, report coverage, observations, report-side findings, evidence source refs, consumer declarations, supported features, repo context, and wiki source. Tax-lot support remains explicitly unsupported until backed by `PortfolioTaxLotWindow:v1` source evidence. No runtime split: this is a reporting read-model contract preservation fix inside the existing service boundary. | Focused reporting read-service tests passed; `make domain-product-validate` passed; monetary-float guard passed with line-anchor-only allowlist refresh; `make check` passed with 559 unit tests; integration/e2e smoke passed with 35 tests. Wiki check-only still reports the known publication drift set. | Updated `contracts/domain-data-products/lotus-report-consumers.v1.json`, `docs/supported-features.md`, repo context, `wiki/Portfolio-Review-Report.md`, and `wiki/Security-and-Governance.md` because holdings source-product/trust posture is public and operator-facing report truth. Wiki publication required after merge. |
| [#130](https://github.com/sgajbi/lotus-report/issues/130) | Hardened | Portfolio-review transactions consumed lotus-core transaction rows but dropped the governed `TransactionLedgerWindow:v1` source-product envelope, settlement date, source quality/reconciliation posture, latest evidence timestamp, restatement/source-lineage fields, and linked transaction cost/cashflow evidence. | Added a bounded source-product anti-corruption mapping layer for transaction windows, surfaced source metadata through `transactions`, `incomeAndActivity`, key figures, client-section items, and evidence source refs, preserved settlement date and linked cost/cashflow evidence at transaction-row level, and degraded supportability for partial, unknown, paged, reconciliation-incomplete, or trust-metadata-incomplete source windows. Same-pattern scan covered summary/review transaction fetch reuse, key figures, coverage, observations, source refs, client sections, supported features, wiki, and repo context. No runtime split: this is a reporting read-model contract preservation fix inside the existing service boundary. | Focused reporting read-service tests passed; `make domain-product-validate` passed; monetary-float guard passed with line-anchor-only allowlist refresh; `make check` passed with 558 unit tests; integration/e2e smoke passed with 35 tests. | Updated `contracts/domain-data-products/lotus-report-consumers.v1.json`, `docs/supported-features.md`, repo context, `wiki/Portfolio-Review-Report.md`, and `wiki/Security-and-Governance.md` because transaction source-product/trust posture is public and operator-facing report truth. Wiki publication required after merge. |
| [#126](https://github.com/sgajbi/lotus-report/issues/126) | Hardened | Portfolio summary P&L synthesized total and unrealized P&L from market-value minus invested-value totals and hard-coded realized P&L to zero, diverging from sourced position unrealized P&L and transaction realized gain/loss already available in review mappings. | Reworked summary `PNL` mapping to fetch lotus-core positions and bounded transaction rows, reuse source-aware holdings and transaction P&L helpers, expose component status, total status, source methodology, and supportability notes, and preserve sourced zero realized P&L only when an upstream realized-gain/loss row explicitly provides zero. Same-pattern scan covered summary `pnlSummary`, review holdings key figures, income/activity realized P&L, transaction realized P&L rows, supported-features docs, and wiki P&L claims. No runtime split: this is an anti-corruption mapping correction inside the existing reporting read service. | Focused reporting read-service tests passed; `make check` passed after the slice. Wiki audit still reports the known legacy bare-URL finding across executable examples. | Updated `docs/supported-features.md`, repo context, `wiki/Portfolio-Review-Report.md`, and `wiki/Security-and-Governance.md` because summary P&L source/supportability semantics are public and operator-facing contract truth. Wiki publication required after merge. |
| [#125](https://github.com/sgajbi/lotus-report/issues/125) | Hardened | Portfolio-review snapshot capture constructed concrete core, performance, risk, and reporting read-service dependencies inside `capture_for_job`, making upstream acquisition policy hard to substitute without monkeypatching module globals. | Added a `PortfolioReviewInputProvider` port, provider result/error envelopes carrying upstream-call evidence, a production `ReportingReadPortfolioReviewInputProvider`, snapshot-store protocol typing, and constructor injection into the capture service factory. Converted key success/failure capture tests to inject fake input providers directly. Same-pattern scan confirmed concrete client/read-service construction moved out of `capture_for_job`. | Focused capture-service tests passed; `make check` passed after the slice. Wiki check-only reports expected publication drift including the changed Architecture page. | Updated repo context, supported-features, and `wiki/Architecture.md` because capture/input-provider ownership is architecture truth. Wiki publication required after merge. |
| [#124](https://github.com/sgajbi/lotus-report/issues/124) | Hardened | PostgreSQL report-job, report-batch, and snapshot/upstream-call adapters opened a new physical connection per operation and had no shared place for pool bounds, acquisition timeout, connect timeout, statement timeout, application name, or shutdown. | Added a bounded process-local PostgreSQL connection provider, provider-backed adapter transaction contexts, shared service/runtime-schema wiring, deterministic FastAPI and runtime-schema shutdown, pool configuration settings, and focused provider/schema/config tests. Same-pattern scan covered report-job, batch, and lineage PostgreSQL adapters. | Focused provider/runtime-schema/PostgreSQL helper tests passed; `make check` passed after the slice. Wiki check-only reports expected publication drift including changed Architecture and Operations pages. | Updated README, repo context, supported-features, `wiki/Architecture.md`, and `wiki/Operations-Runbook.md` because PostgreSQL pool configuration and ownership are operator-facing runtime truth. Wiki publication required after merge. |
| [#129](https://github.com/sgajbi/lotus-report/issues/129) | Hardened | Report-job and batch lifecycle rules were duplicated across PostgreSQL runtime ledgers and SQLite test adapters, leaving transition, cancellation, retry/failure, and terminal reconciliation policy embedded in persistence code. | Added shared report-job lifecycle policy and shared batch lifecycle policy; routed both PostgreSQL and SQLite adapters through the same transition/cancellation, batch item failure outcome, and terminal batch reconciliation helpers; added focused policy tests. Same-pattern scan confirmed the duplicate `allowed_from` and batch reconciliation branches were removed from the adapters. | Focused lifecycle policy and adapter tests passed; `make check` passed after the slice. Wiki check-only reports expected publication drift including the changed Architecture page. | Updated repo context, supported-features, and `wiki/Architecture.md` because lifecycle policy ownership is architecture/operator truth. Wiki publication required after merge. |
| [#127](https://github.com/sgajbi/lotus-report/issues/127) | Hardened | Report-job lifecycle events carried identifiers only in human-readable messages, forcing replay/regenerate/batch lineage code to parse text and leaving no versioned support-safe event payload contract for operators or future migrations. | Added `report-status-event.v1` with `event_schema_version`, `event_family`, typed support-safe payloads, optional event idempotency keys, migration/read compatibility for legacy rows, and payload validation for accepted, failed, render/archive, rerender, regenerate/replay, and batch-item lineage events. Same-pattern scan removed message parsing from replay/regenerate/batch replay lineage and preserved human messages only for operator readability. No runtime split: this is an internal ledger contract hardening inside the existing report job boundary. | Focused report-job ledger, PostgreSQL helper, batch replay, portfolio-memory event, and report-job API tests passed; `make domain-product-validate` passed; `make check` passed with 560 unit tests after lint, format-check, monetary-float guard, idea evidence gates, mypy, and OpenAPI quality. Wiki check-only still reports the known publication drift set. | Updated supported-features, repo context, `wiki/Operations-Runbook.md`, `wiki/API-Surface.md`, and platform backend skill guidance because lifecycle event payload contracts are operator-facing and future-agent delivery truth. Wiki publication required after merge. |
| [#114](https://github.com/sgajbi/lotus-report/issues/114) | Hardened | Regenerate and replay returned source/derived job ids at POST time and wrote typed lifecycle events, but there was no durable first-class relationship row that support diagnostics could query later from either the source or derived job. | Added `report_job_relationship` persistence, SQLite/PostgreSQL upsert/list APIs, readiness migration guard, diagnostics projection, regenerate/replay/batch-item relationship writes, and tests covering source-to-derived plus derived-to-source lookup for regenerate success, regenerate failure, replay success, replay failed-render, and batch-item replay. Same-pattern scan aligned batch-item replay with job-level replay. No runtime split: this is a ledger/read-model hardening inside the existing report job boundary. | Focused report-job ledger, PostgreSQL helper, replay service, batch replay, PostgreSQL integration, and report-job API tests passed; same-pattern scan found no lifecycle message/idempotency parsing paths; `make domain-product-validate` passed; `make check` passed with 562 unit tests after lint, format-check, monetary-float guard, idea evidence gates, mypy, and OpenAPI quality. Wiki check-only still reports the known publication drift set. | Updated supported-features, repo context, `wiki/Operations-Runbook.md`, and `wiki/API-Surface.md` because source/derived report-job relationships are operator-facing diagnostics truth. Wiki publication required after merge. |
| [#113](https://github.com/sgajbi/lotus-report/issues/113) | Hardened | Rerender attempts were persisted as first-class rows and returned at POST time, but operators could not rediscover archived or failed rerender state from diagnostics after the initial command response was unavailable. | Added bounded SQLite/PostgreSQL `list_rerender_attempts` reads, a diagnostics-only `ReportRerenderAttemptDiagnostics` DTO that excludes idempotency keys, correlation/trace values, storage keys, and raw payloads, and diagnostics projection for recent rerender attempts. Same-pattern scan confirmed rerender rediscovery now uses persisted attempt rows instead of event-message inference. No runtime split: this is a support-safe read-model hardening inside the existing report job ledger boundary. | Focused report-job ledger and PostgreSQL helper tests passed; focused rerender API tests passed for archived and failed attempt rediscovery; mypy passed; `make domain-product-validate` passed; `make check` passed with 564 unit tests after lint, format-check, monetary-float guard, idea evidence gates, mypy, and OpenAPI quality. | Updated supported-features, repo context, `wiki/Operations-Runbook.md`, and `wiki/API-Surface.md` because rerender correction-document audit is operator-facing diagnostics truth. Wiki publication required after merge. |
| [#110](https://github.com/sgajbi/lotus-report/issues/110) | Hardened | Manage-owned bounded `portfolio_memory_context` was persisted in the raw immutable snapshot, but render packages and support-safe lineage summaries dropped context hash, support boundary, event-ref limit/selection/returned/omitted/truncated posture, and per-ref event time/rank. | Preserved bounded portfolio-memory controls in `report_data.portfolio_memory` for proof-pack, rebalance-wave, and outcome-review render packages and added support-safe lineage-summary fields for context hash and truncation posture. Same-pattern tests cover all three DPM render package builders and capture lineage without reconstructing Manage-owned timeline events. No runtime split: this is a projection/readiness hardening inside the existing report service boundary. | Focused render-package and lineage-capture tests passed with 52 tests; ruff passed; mypy passed; `make domain-product-validate` passed; `make check` passed with 564 unit tests after lint, format-check, monetary-float guard, idea evidence gates, mypy, and OpenAPI quality. Wiki check-only still reports expected publication drift including changed DPM pages. | Updated supported-features, repo context, `wiki/API-Surface.md`, `wiki/Proof-Pack-Report.md`, and `wiki/Rebalance-Wave-Report.md` because bounded portfolio-memory lineage controls are downstream render/archive and operator-facing truth. Wiki publication required after merge. |
| [#111](https://github.com/sgajbi/lotus-report/issues/111) | Hardened | `ClientReportEvidencePack:v1` was active and its trust telemetry claimed complete/current/quality-passed/unblocked posture while `lotus-performance` and `lotus-risk` analytics dependencies were deliberately absent from governed consumer declarations and documented as watchlisted. | Changed the product declaration to separate governed `lotus-core` evidence from analytics-enriched performance/risk evidence, set product completeness default to partial, and added a machine-readable dependency certification boundary. Downgraded the trust telemetry snapshot to partial/quality-warning/blocked for analytics-enriched certification until upstream producer declarations approve `lotus-report`. Added alignment tests across producer declaration, consumer declaration, telemetry, and README watchlist. No runtime section removal: this is a contract-truth and capability-publication hardening slice. | Focused domain-data-product contract tests passed with 8 tests; `make domain-product-validate` passed; ruff passed; mypy passed; `make check` passed with 566 unit tests after lint, format-check, monetary-float guard, idea evidence gates, mypy, and OpenAPI quality. Wiki check-only still reports expected publication drift including changed mesh/product pages. | Updated supported-features, repo context, `wiki/Mesh-Data-Products.md`, and `wiki/Portfolio-Review-Report.md` because evidence-pack certification posture is catalog-, operator-, and consumer-facing truth. Wiki publication required after merge. |
| [#115](https://github.com/sgajbi/lotus-report/issues/115) | Hardened | Outcome-review, proof-pack, and rebalance-wave DPM handoff requests were raw dictionaries, allowing missing source hashes/evidence refs/redaction/retention/supportability fields to be captured and rendered as complete snapshots with placeholder lineage. | Added typed source-owned `DpmOutcomeReportInput`, `DpmProofPackReportInput`, and `DpmWaveReportInput` schemas with required SHA-256 source hashes, evidence refs, redaction policy, retention policy, supportability posture, and relevant section/item state. Ledger serialization preserves the existing option payload contract through `model_dump(mode="json")`. Render-package construction now rejects corrupted legacy DPM snapshots with missing required lineage instead of substituting `not_available`, and render orchestration maps that to `render_validation_failed`. Same-pattern fix aligned IDEA evidence proof-pack materialization to carry the source retention policy into the typed request. No runtime split: this is a contract-boundary and render-readiness hardening inside the existing report service. | Focused report-job ledger, lineage capture, render-package, API rejection, OpenAPI schema, and IDEA evidence materialization tests passed with 153 tests; mypy passed; `make domain-product-validate` passed; `make check` passed with 564 unit tests after lint, format-check, monetary-float guard, idea evidence gates, mypy, and OpenAPI quality. Wiki check-only still reports expected publication drift including changed DPM pages. | Updated supported-features, repo context, `wiki/API-Surface.md`, `wiki/Proof-Pack-Report.md`, and `wiki/Rebalance-Wave-Report.md` because DPM request contract semantics are public and operator-facing report truth. Wiki publication required after merge. |
| [#120](https://github.com/sgajbi/lotus-report/issues/120) | Hardened | Portfolio summary/review transaction windows paged lotus-core until source `total` was reached, with no report-owned row/page budget or visible truncation state for oversized or inconsistent windows. | Added `REPORT_TRANSACTION_MAX_ROWS` and `REPORT_TRANSACTION_MAX_PAGES`; introduced a bounded transaction paging result with source total, fetched pages, and supportability; surfaced truncation through summary supportability, review section status, readiness, key figures, report coverage, observations, and report-side findings. Same-pattern scan covered summary and review transaction consumers and existing supportability/coverage paths. No runtime split: this is a service-internal pagination policy. | Focused reporting-read-service/config tests passed; `make check` passed after the slice. Wiki audit structural findings on the changed Portfolio Review page were fixed; residual audit output is the known bare-URL finding in executable examples. | Updated README, `docs/supported-features.md`, repo context, and `wiki/Portfolio-Review-Report.md` because transaction-window supportability is product/operator-facing report truth. Wiki publication required after merge. |
| [#119](https://github.com/sgajbi/lotus-report/issues/119) | Hardened | Shared downstream HTTP retries consumed retry budget for transport exceptions only; transient HTTP statuses `429`, `502`, `503`, and `504` returned immediately and could fail report workflows during recoverable downstream overload. | Refactored `post_with_retry` and `get_with_retry` through one shared request loop, added a bounded retryable-status policy, bounded `Retry-After` handling, exhausted-status diagnostics, and tests for POST/GET transient retry, exhausted status retry, non-transient passthrough, and Retry-After delay. Same-pattern scan covered all downstream clients using the shared helper; no per-client retry branches were introduced. | Focused HTTP resilience tests passed; `make check` passed after the slice. | Updated `docs/standards/durability-consistency.md` and `REPOSITORY-ENGINEERING-CONTEXT.md` because downstream retry policy is shared runtime truth. No wiki change: operator replay/runbook pages already discuss report-job replay, not low-level upstream retry policy. |
| [#121](https://github.com/sgajbi/lotus-report/issues/121) | Hardened | Enterprise authorization enforcement was opt-in for both read and write paths, so production-like direct service deployments could silently run with authz disabled or missing primary key identity material. | Added a centralized enterprise runtime profile helper, production-like profile detection, fail-closed read/write authorization helpers, startup validation failures for disabled production authz or missing primary key material, and tests proving explicit local permissiveness plus production read/write denial. Same-pattern scan covered README, enterprise readiness standard, wiki runbook, security wiki, and repo context direct-service guidance. No runtime split: this remains one deployable service with a hardened internal middleware boundary. | Focused enterprise-readiness tests passed; `make check` passed after the slice. | Updated `README.md`, `docs/standards/enterprise-readiness.md`, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/Security-and-Governance.md`, `wiki/Operations-Runbook.md`, and `wiki/Getting-Started.md` because direct-service authz posture is operator- and agent-facing runtime truth. Wiki publication required after merge. |
| [#117](https://github.com/sgajbi/lotus-report/issues/117) | Hardened | Enterprise audit middleware enforced write payload limits only when `Content-Length` declared an oversized body, and accepted missing or malformed lengths. | Added malformed `Content-Length` rejection, actual write-body stream counting, oversized streamed/missing-length rejection, underdeclared body rejection, and downstream body replay for valid requests. | Focused enterprise-readiness middleware tests passed; `make check` passed after the slice. | Updated `wiki/Security-and-Governance.md` because direct-service HTTP boundary posture changed. Wiki publication required after merge. |
| [#128](https://github.com/sgajbi/lotus-report/issues/128) | Hardened | Operator-facing wiki examples omitted caller-context, correlation/trace, or idempotency details required by report-job and batch routers. | Added a reusable `report-operator-headers.curl` caller-context config to API and operations wiki pages; routed report-job, support-safe read, cancel, rerender, regenerate, replay, batch materialization, batch control, batch item replay, and run-once examples through it; added docs contract tests for caller-context config and idempotency-key examples. | Focused operator-runbook docs test passed; `make check` passed after the slice. Wiki audit structural findings in changed pages were fixed; residual audit findings are bare-URL reports in executable curl examples. | Updated `wiki/API-Surface.md` and `wiki/Operations-Runbook.md` because operator command examples are user-facing support truth. Wiki publication required after merge. |
| [#132](https://github.com/sgajbi/lotus-report/issues/132) | Hardened | The review playbook and repo context did not tell agents to search/reuse GitHub issues, update the issue-discovery ledger, or keep active backlog state out of local-only docs. | Added GitHub issue-discovery workflow steps, required issue fields, duplicate-search proof, #109 ledger comment format, and active-backlog versus historical-ledger guidance; updated repo context and wiki navigation; added docs regression coverage for these controls. | Focused docs guidance test passed; `make check` passed after the slice. Wiki audit direct findings in touched pages were fixed; residual audit findings are legacy bare-URL reports in executable curl/example pages outside this issue's scope. | Updated `wiki/Development-Workflow.md` because review-to-issue workflow is agent/operator-facing delivery truth; normalized `_Sidebar.md` title and `Security-and-Governance.md` wording found during wiki audit. Wiki publication required after merge. |
| [#123](https://github.com/sgajbi/lotus-report/issues/123) | Hardened | Feature, PR, and main workflows duplicated raw pytest/coverage commands even though Makefile owns the repository-native test and coverage contract. | Added `test-suite-coverage` and `coverage-gate` Make targets; updated Feature Lane, PR Merge Gate, and Main Releasability workflows to consume Make targets; added workflow contract tests blocking raw pytest/coverage command drift. | Focused workflow contract tests passed; `make test-suite-coverage TEST_SUITE=unit TEST_PATH=tests/unit` passed; `make check` passed after the slice. | No wiki change: existing wiki command mapping (`make check`, `make ci`, coverage floor) remains true; workflows now align to that documented command contract. |
| [#122](https://github.com/sgajbi/lotus-report/issues/122) | Hardened | `make security-audit` carried raw inline `pip-audit --ignore-vuln` flags without owner, expiry, linked issue, or deterministic expiration enforcement. | Added governed dependency vulnerability exception records; added a security-audit runner that validates non-expired, issue-linked exceptions before invoking `pip-audit`; updated Makefile routing and tests to reject raw ignore drift. | Focused dependency-exception tests passed; `make security-audit` passed; `make check` passed after the slice. | Updated `docs/standards/dependency-vulnerability-exceptions.md` and `wiki/Security-and-Governance.md` because security exception governance is operator/reviewer-facing truth. Wiki publication required after merge. |
| [#112](https://github.com/sgajbi/lotus-report/issues/112) | Hardened | Batch-item failed-work replay did not emit the documented bounded replay operation metric, leaving operators with job-level replay telemetry but no batch-item replay pressure signal. | Reused existing `operation="replay_command"` metric vocabulary for accepted/idempotent batch-item replay and rejected replay attempts; added integration assertions for accepted, conflict, missing-key, and missing-item metric labels without forbidden identifiers. | Focused batch replay API tests passed; `make check` passed after the slice. | No wiki/docs change: the existing operations docs already describe failed-work replay under `lotus_report_operations_total{operation="replay_command"}` and the implementation now matches that published contract. |
| [#116](https://github.com/sgajbi/lotus-report/issues/116) | Hardened | `ReportingReadService` and lineage capture raised or classified FastAPI `HTTPException` inside application behavior, coupling report orchestration to HTTP delivery. | Added typed reporting application errors; moved `/reports` HTTP translation into the router; updated lineage failure classification to use application failure categories; updated service tests to assert application errors and API tests to assert HTTP mapping. | `make check` passed; focused service/lineage/API tests passed; boundary scan found no FastAPI imports or `HTTPException` assertions in the service and lineage paths covered by #116. | No wiki change: external API behavior is preserved; this is an internal architecture-boundary refactor. Monetary-float allowlist was refreshed only for line-number drift caused by this service refactor, with prior review dates preserved. |
