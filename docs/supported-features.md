# Supported Features

This file is the durable repository source for implementation-backed `lotus-report` product
capabilities.

It separates shipped behavior from planned RFC work. Do not describe a feature as
implementation-backed unless code, tests, API contract, documentation, and operational evidence
exist.

## Support States

| State | Meaning |
| --- | --- |
| `implementation-backed` | The feature is implemented, tested, documented, and reflected by the relevant API/capability surface. |
| `planned` | The feature is proposed or accepted in an RFC but is not yet shipped behavior. |
| `deprecated` | The feature still exists but is scheduled for removal or replacement. |
| `not-supported` | The feature is explicitly outside current product scope. |

## Implementation-Backed Features

| Feature key | Surface | Evidence | Notes |
| --- | --- | --- | --- |
| `lotus-report.reporting.portfolio_summary` | `POST /reports/portfolios/{portfolio_id}/summary` | `src/app/routers/reports.py`, `src/app/services/reporting_read_service.py`, `tests/integration/test_api.py` | Current reporting summary aggregation capability. |
| `lotus-report.reporting.portfolio_review` | `POST /reports/portfolios/{portfolio_id}/review` | `src/app/routers/reports.py`, `src/app/services/reporting_read_service.py`, `tests/unit/test_reporting_read_service.py`, `tests/integration/test_api.py`, `tests/e2e/test_reporting_workflows.py` | Current portfolio review report route for front-office meeting-pack workflows. |
| `lotus-report.reporting.portfolio_review.first_class.v1` | `POST /reports/portfolios/{portfolio_id}/review` typed request/response | `src/app/models/contracts.py`, `src/app/routers/reports.py`, `tests/integration/test_api.py`, `tests/e2e/test_reporting_workflows.py` | Versioned `PortfolioReviewReportRequest` and `PortfolioReviewReportResponse` with OpenAPI-governed full request/response examples, snake_case public fields, report metadata, `review_period`, `reporting_currency`, audience posture, and structured disclosures. |
| `lotus-report.reporting.portfolio_review.section_readiness.v1` | Portfolio review report response `client_sections` | `src/app/services/reporting_read_service.py`, `tests/unit/test_reporting_read_service.py`, `tests/unit/test_reporting_read_service_additional.py` | Ordered client section envelope with `ready`, `partial`, `unavailable`, `omitted_by_request`, and `not_applicable` states plus normalized machine-readable section items for measures, allocation buckets, performance periods, risk periods, income/activity, holdings, and categorized transactions. |
| `lotus-report.reporting.portfolio_review.supportability.v1` | Portfolio review report response `readiness`, section status, `performance.supportability`, and `riskAnalytics.supportability` | `src/app/services/reporting_read_service.py`, `src/app/services/portfolio_review_advisor.py`, `tests/unit/test_reporting_read_service.py`, `tests/unit/test_reporting_read_service_additional.py`, `tests/unit/test_portfolio_review_advisor.py` | Source-backed supportability model that marks benchmark-dependent performance and risk output as ready when benchmark return series are sourced, partial when they are not sourced, prioritizes blocking and warning notes over informational notes, and carries advisor-useful limitation language. |
| `lotus-report.reporting.portfolio_review.key_figures.v1` | Portfolio review report response `key_figures`, `review_observations`, and `report_coverage` | `src/app/models/contracts.py`, `src/app/services/reporting_read_service.py`, `src/app/routers/integration.py`, `tests/unit/test_reporting_read_service.py`, `tests/unit/test_reporting_read_service_additional.py`, `tests/integration/test_api.py` | Normalized front-office key figures for portfolio value, allocation, performance, benchmark return, benchmark-relative return, risk, beta, tracking error, information ratio, income/activity, holdings concentration, transaction categories, transaction-level realized P&L, position P&L coverage, and contribution coverage. Also identifies unavailable gold-standard figures such as targets/guidelines/suitability and tax-lot jurisdiction treatment when not sourced. |
| `lotus-report.reporting.portfolio_review.position_pnl.v1` | Portfolio review response `holdings`, `client_sections`, `key_figures.holdings`, and `report_coverage` | `src/app/services/reporting_read_service.py`, `tests/unit/test_reporting_read_service.py`, `tests/unit/test_reporting_read_service_additional.py` | Sourced lotus-core HoldingsAsOf position enrichment including ISIN, product type, sector, country of risk, rating, liquidity tier, held-since date, market price, cost basis, local and reporting-currency market value, local and reporting-currency unrealized P&L, and unrealized P&L percentage. |
| `lotus-report.reporting.portfolio_review.transaction_realized_pnl.v1` | Portfolio review response `income_and_activity`, `transactions`, `key_figures`, and `report_coverage` | `src/app/services/reporting_read_service.py`, `tests/unit/test_reporting_read_service.py`, `tests/unit/test_reporting_read_service_additional.py` | Sourced lotus-core TransactionLedgerWindow realized gain/loss at transaction level, with realized P&L totals, gain/loss split, largest realized gain/loss, and transaction-row realized P&L enrichment. Open tax-lot attribution and jurisdiction-specific tax treatment remain explicit upstream gaps. |
| `lotus-report.reporting.portfolio_review.performance_contribution.v1` | Portfolio review response `performance.contribution`, `client_sections`, `key_figures.performance`, and `report_coverage` | `src/app/clients/performance_client.py`, `src/app/services/reporting_read_service.py`, `tests/unit/test_clients.py`, `tests/unit/test_reporting_read_service.py`, `tests/unit/test_reporting_read_service_additional.py` | Sourced lotus-performance YTD contribution view with total contribution, top position contributors, asset-class and sector hierarchy contribution rows, contribution coverage, and matching holding-row contribution enrichment when positions are in the same response. |
| `lotus-report.reporting.portfolio_review.source_backed_risk_free.v1` | Portfolio review response `risk_analytics`, `upstream_capability_audit`, and `client_sections` | `src/app/services/reporting_read_service.py`, `src/app/routers/integration.py`, `tests/unit/test_reporting_read_service.py`, `tests/unit/test_reporting_read_service_additional.py`, `tests/integration/test_api.py` | Portfolio review risk analytics uses lotus-risk stateful sourcing so Sharpe/risk-adjusted return can apply the lotus-core mastered risk-free series through the governed risk/performance/core path. ZERO-rate treatment remains visible as a supportability gap when upstream risk cannot source the rate. |
| `lotus-report.reporting.portfolio_review.source_backed_benchmark.v1` | Portfolio review response `performance`, `risk_analytics`, `key_figures`, `report_coverage`, `upstream_capability_audit`, and `client_sections` | `src/app/services/reporting_read_service.py`, `src/app/routers/integration.py`, `tests/unit/test_reporting_read_service.py`, `tests/integration/test_api.py` | Portfolio review passes `benchmark_code` into lotus-performance workspace summary and lotus-risk stateful analytics, enabling source-backed benchmark return, benchmark-relative return, beta, tracking error, information ratio, and benchmark-relative risk when the upstream benchmark series is available. Missing upstream benchmark series remains visible as a supportability gap. |
| `lotus-report.reporting.portfolio_review.client_profile.v1` | Portfolio review response `client_profile`, `key_figures.client_profile`, `report_coverage`, and `evidence.source_refs` | `src/app/clients/core_query_client.py`, `src/app/services/reporting_read_service.py`, `src/app/models/contracts.py`, `tests/unit/test_clients.py`, `tests/unit/test_reporting_read_service.py` | Sourced lotus-core portfolio detail context for client id, advisor id, booking center, objective, risk exposure, investment horizon, mandate type, leverage permission, portfolio status, open date, base currency, and cost-basis method. Missing profile fields are explicit gaps rather than inferred client facts. |
| `lotus-report.reporting.portfolio_review.advisor_briefing.v1` | Portfolio review response `advisor_briefing` and `report_structure` | `src/app/services/reporting_read_service.py`, `src/app/models/contracts.py`, `tests/unit/test_reporting_read_service.py` | Deterministic advisor-only meeting briefing and presentation sequence that organizes the report around client context, portfolio snapshot, performance drivers, risk controls, activity, appendices, evidence, and required advisor checks. |
| `lotus-report.reporting.portfolio_review.ai_readiness.v1` | Portfolio review response `ai_readiness` and `report_coverage` | `src/app/services/reporting_read_service.py`, `src/app/models/contracts.py`, `tests/unit/test_reporting_read_service.py` | Guarded AI-readiness metadata for grounded meeting assistance. It does not generate advice; trade recommendations, suitability determinations, and inferred client profiles remain blocked until governed `lotus-ai`, advisory evidence, and human approval are in place. |
| `lotus-report.reporting.portfolio_review.upstream_capability_audit.v1` | Portfolio review response `upstream_capability_audit` | `src/app/services/reporting_read_service.py`, `src/app/models/contracts.py`, `tests/unit/test_reporting_read_service.py`, `tests/integration/test_api.py` | Machine-readable certification audit separating source-backed report capabilities from upstream gaps such as target allocation and suitability controls, open tax-lot jurisdiction treatment, benchmark return series when not sourced, and risk-free rate availability when upstream risk still reports zero-rate treatment. |
| `lotus-report.reporting.portfolio_review.evidence_pack.v1` | Portfolio review report response `evidence` | `src/app/services/reporting_read_service.py`, `contracts/domain-data-products/lotus-report-products.v1.json`, `tests/unit/test_domain_data_product_contracts.py`, `tests/unit/test_reporting_read_service.py` | `ClientReportEvidencePack` lineage bundle id, source refs, trust metadata, and domain-product completeness alignment. |
| `lotus-report.reporting.portfolio_review.advisor_sections.v1` | `POST /reports/portfolios/{portfolio_id}/review` response `advisor_sections` | `src/app/services/portfolio_review_advisor.py`, `src/app/services/reporting_read_service.py`, `src/app/models/contracts.py`, `tests/unit/test_reporting_read_service.py`, `tests/unit/test_reporting_read_service_additional.py` | Advisor-only deterministic discussion prompts and non-mutating Workbench, performance, risk, proposal, and action-register route targets. |
| `lotus-report.reporting.portfolio_review.workbench_ready.v1` | Gateway-facing portfolio review route and route targets | `src/app/services/portfolio_review_advisor.py`, `sgajbi/lotus-gateway#145`, `tests/integration/test_api.py` | Gateway-readiness proof for Workbench consumers. No Workbench UI preview is shipped yet; advisor-only material remains separated at the report and gateway boundaries. |
| `lotus-report.reporting.portfolio_review.job_ledger.v1` | `POST /reports/portfolio-reviews`, `GET /reports/jobs`, `GET /reports/jobs/{job_id}` | `src/app/reporting_jobs/postgres_ledger.py`, `migrations/001_report_job_ledger.sql`, `migrations/005_report_job_render_lifecycle.sql`, `migrations/006_report_job_archive_handoff.sql`, `src/app/routers/report_jobs.py`, `tests/unit/reporting_jobs/test_report_job_ledger.py`, `tests/integration/test_report_job_api.py` | PostgreSQL-backed durable report request, job, and append-only status-event ledger for portfolio review report job initiation and operational lookup, including render and archive lifecycle states plus persisted render/archive metadata for PDF jobs. |
| `lotus-report.reporting.portfolio_review.idempotent_job_create.v1` | `POST /reports/portfolio-reviews` with `Idempotency-Key` | `src/app/reporting_jobs/postgres_ledger.py`, `migrations/001_report_job_ledger.sql`, `tests/unit/reporting_jobs/test_report_job_ledger.py`, `tests/integration/test_report_job_api.py` | Idempotent portfolio review report job creation using a caller idempotency key, deterministic request hash, and database-level uniqueness. Duplicate key/hash returns the existing job; duplicate key with a different request returns a deterministic conflict. |
| `lotus-report.reporting.portfolio_review.job_status.v1` | `GET /reports/jobs/{job_id}` | `src/app/reporting_jobs/models.py`, `src/app/routers/report_jobs.py`, `tests/integration/test_report_job_api.py` | Product-safe report job status retrieval with report type, portfolio scope, status, failure category, retry eligibility, cancellation flag, correlation id, and trace id without exposing database internals or worker topology. |
| `lotus-report.reporting.operations.job_diagnostics.v1` | `GET /reports/jobs/{job_id}/diagnostics` | `src/app/reporting_jobs/models.py`, `src/app/routers/report_jobs.py`, `tests/integration/test_report_job_api.py` | RFC-0105 Slice 4 source-backed operator diagnostics view that composes report job status, latest lifecycle event, snapshot posture, upstream-lineage summary, render metadata, archive handoff identifiers, and related operation links without exposing raw snapshot payloads, storage references, upstream request/response payloads, database internals, or mutating replay controls. |
| `lotus-report.reporting.operations.rerender_from_snapshot.v1` | `POST /reports/jobs/{job_id}/rerender` | `src/app/reporting_render/rerender_service.py`, `src/app/reporting_jobs/postgres_ledger.py`, `migrations/008_report_rerender_attempt.sql`, `src/app/routers/report_jobs.py`, `tests/integration/test_report_job_api.py` | RFC-0105 Slice 5 idempotent rerender command for already archived PDF report jobs. The command reuses the existing immutable snapshot, preserves snapshot id and hash, creates a new rerender attempt/render identity, records source-job lifecycle audit events, and archives the new document as an explicit correction without recollecting upstream data. |
| `lotus-report.reporting.operations.regenerate_from_upstream.v1` | `POST /reports/jobs/{job_id}/regenerate` | `src/app/reporting_render/regenerate_service.py`, `src/app/reporting_render/service.py`, `src/app/routers/report_jobs.py`, `tests/integration/test_report_job_api.py`, `tests/unit/test_observability.py` | RFC-0105 Slice 6 idempotent regenerate command for already archived PDF report jobs. The command creates a new report job from the source request, recollects upstream data into a fresh snapshot and lineage bundle, archives the new document as an explicit replacement, and returns old/new job, snapshot, hash, and archive document identities for audit comparison. |
| `lotus-report.reporting.operations.failed_work_replay.v1` | `POST /reports/jobs/{job_id}/replay`, `POST /reports/batches/{batch_id}/items/{batch_item_id}/replay` | `src/app/reporting_render/replay_service.py`, `src/app/report_batch_orchestrator/replay.py`, `src/app/report_batch_orchestrator/ledger.py`, `src/app/routers/report_jobs.py`, `src/app/routers/report_batches.py`, `tests/integration/test_report_job_api.py`, `tests/integration/test_report_batch_api.py`, `tests/unit/test_observability.py` | RFC-0105 Slice 7 failed-work replay for failed retry-eligible report jobs and implementation-backed RFC-0104 batch items. Replay creates or reuses a replay-scoped report job, preserves lineage through source/replay lifecycle events, rejects completed, archived, cancelled, leased, terminal, or non-retryable work, and does not duplicate already archived documents. Scheduler CRUD, registry mutation, distribution, and archive housekeeping remain outside this command. |
| `lotus-report.reporting.portfolio_review.job_search.v1` | `GET /reports/jobs` | `src/app/reporting_jobs/models.py`, `src/app/reporting_jobs/postgres_ledger.py`, `src/app/routers/report_jobs.py`, `tests/integration/test_report_job_api.py` | Operator-safe bounded report job search by tenant, region, status, report type, portfolio id, as-of date, idempotency key, correlation id, and created-at window. Results are support-safe summaries and do not expose SQL, worker topology, or raw internals. |
| `lotus-report.reporting.portfolio_review.job_event_history.v1` | `GET /reports/jobs/{job_id}/events` | `src/app/reporting_jobs/models.py`, `src/app/reporting_jobs/postgres_ledger.py`, `src/app/routers/report_jobs.py`, `tests/integration/test_report_job_api.py` | Support-facing append-only report job event history for audit diagnostics and operational troubleshooting. |
| `lotus-report.reporting.portfolio_review.pre_render_cancel.v1` | `POST /reports/jobs/{job_id}/cancel` | `src/app/reporting_jobs/postgres_ledger.py`, `src/app/routers/report_jobs.py`, `tests/unit/reporting_jobs/test_report_job_ledger.py`, `tests/integration/test_report_job_api.py` | Bounded cancellation for jobs that have not reached `rendering`, completion, or later archive phases. Once a PDF handoff is in flight, cancellation is rejected to preserve render-side determinism and auditability. |
| `lotus-report.reporting.portfolio_review.render_submission.v1` | `POST /reports/portfolio-reviews`, `GET /reports/jobs/{job_id}` | `src/app/reporting_render/service.py`, `src/app/clients/render_client.py`, `src/app/reporting_jobs/postgres_ledger.py`, `migrations/005_report_job_render_lifecycle.sql`, `tests/unit/reporting_render/test_service.py`, `tests/integration/test_report_job_api.py`, `scripts/rfc_0102_live_evidence.py` | PDF-capable portfolio review jobs assemble a governed render package from the durable snapshot, submit it to `lotus-render` with correlation and trace headers, persist render metadata and bounded determinism fingerprint, and surface render success or failure through the report-job status contract. The supported determinism claim is the bounded runtime-envelope fingerprint, not byte-stable PDF identity. First-wave rerender, regenerate, and failed-work replay controls are implementation-backed under separate operation keys; distribution remains out of scope. |
| `lotus-report.reporting.portfolio_review.archive_handoff.v1` | `POST /reports/portfolio-reviews`, `GET /reports/jobs/{job_id}`, `GET /reports/jobs/{job_id}/events` | `src/app/reporting_render/service.py`, `src/app/clients/archive_client.py`, `src/app/reporting_jobs/postgres_ledger.py`, `migrations/006_report_job_archive_handoff.sql`, `tests/unit/clients/test_archive_client.py`, `tests/unit/reporting_render/test_service.py`, `tests/unit/reporting_jobs/test_report_job_ledger.py`, `tests/integration/test_report_job_api.py` | After successful PDF render completion, `lotus-report` hands the rendered artifact and source-backed report metadata to `lotus-archive`, records `archiving` and `archived` lifecycle events separately from render completion, and maps archive validation, conflict, storage, and execution failures truthfully. Retrieval, retention execution, legal hold, purge, and document distribution remain owned by `lotus-archive`. |
| `lotus-report.reporting.outcome_review.job_orchestration.v1` | `POST /reports/outcome-reviews`, `GET /reports/jobs/{job_id}`, `GET /reports/jobs/{job_id}/snapshot`, `GET /reports/jobs/{job_id}/lineage` | `src/app/routers/report_jobs.py`, `src/app/reporting_jobs/ledger.py`, `src/app/reporting_jobs/postgres_ledger.py`, `src/app/reporting_lineage/capture_service.py`, `src/app/reporting_render/package_builder.py`, `tests/integration/test_report_job_api.py`, `tests/unit/reporting_render/test_service.py` | RFC-0042 post-trade outcome-review report job orchestration. The endpoint accepts manage-owned `DpmOutcomeReportInput`, persists that bounded payload as the immutable report snapshot, records lineage back to `lotus-manage`, builds an `outcome_review` render package for `lotus-render`, and uses the existing archive handoff lifecycle for PDF artifacts. lotus-report does not recompute expected-versus-realized outcome truth. |
| `lotus-report.reporting.portfolio_review.input_snapshot.v1` | Durable snapshot capture on `POST /reports/portfolio-reviews` | `src/app/reporting_lineage/capture_service.py`, `src/app/reporting_lineage/postgres_store.py`, `migrations/003_report_input_snapshot.sql`, `tests/unit/reporting_lineage/test_report_input_snapshot_store.py`, `tests/integration/test_report_job_api.py`, `tests/integration/test_postgres_report_input_snapshot_store.py` | Durable one-to-one report input snapshot capture linked to the report job, with canonical hashing, immutable storage, supportability posture, and snapshot summary metadata. |
| `lotus-report.reporting.portfolio_review.upstream_lineage.v1` | Durable upstream-call evidence on `POST /reports/portfolio-reviews` | `src/app/reporting_lineage/capture_service.py`, `src/app/reporting_lineage/postgres_store.py`, `migrations/004_report_upstream_call.sql`, `tests/unit/reporting_lineage/test_report_input_snapshot_store.py`, `tests/integration/test_report_job_api.py`, `tests/integration/test_postgres_report_input_snapshot_store.py` | Append-only upstream-call lineage rows for lotus-core, lotus-performance, and lotus-risk with request hash, response hash or ref, status code, latency, and explicit supportability/completeness posture. |
| `lotus-report.reporting.portfolio_review.snapshot_lookup.v1` | `GET /reports/jobs/{job_id}/snapshot`, `GET /reports/snapshots/{snapshot_id}` | `src/app/routers/report_jobs.py`, `src/app/reporting_lineage/models.py`, `tests/integration/test_report_job_api.py` | Support-safe snapshot lookup by job id or snapshot id without exposing raw upstream internals or database implementation details. |
| `lotus-report.reporting.portfolio_review.lineage_lookup.v1` | `GET /reports/jobs/{job_id}/lineage`, `GET /reports/snapshots/{snapshot_id}/lineage` | `src/app/routers/report_jobs.py`, `src/app/reporting_lineage/models.py`, `tests/integration/test_report_job_api.py` | Certified support-safe lineage lookup that returns the durable snapshot together with append-only upstream-call evidence for audit and operational diagnostics. |
| `report.observability.evidence_surface_supportability` | `GET /integration/capabilities` and `/metrics` | `src/app/routers/integration.py`, `src/app/reporting_metrics.py`, `tests/integration/test_api.py`, `tests/unit/test_observability.py`, `tests/unit/test_integration_capabilities.py` | RFC-0108 evidence-surface supportability posture for Gateway and Workbench consumers. The integration capability response publishes bounded `supportability.state`, `supportability.reason`, `supportability.freshness_bucket`, evidence-feature counts, and workflow counts, while `/metrics` emits `lotus_report_evidence_surface_supportability_total` with only `state`, `reason`, and `freshness_bucket` labels. |
| `lotus-report.reporting.batch_materialization_api.v1` | `POST /reports/batches`, `GET /reports/batches/{batch_id}` | `src/app/routers/report_batches.py`, `src/app/report_batch_orchestrator/ledger.py`, `src/app/report_batch_orchestrator/postgres_ledger.py`, `tests/integration/test_report_batch_api.py`, `tests/integration/test_postgres_report_batch_ledger.py` | Operator-safe batch materialization and status APIs for explicit portfolio-list, selected-subset, all-active, and manifest batches from source-backed eligible portfolio candidates. The APIs persist durable batch and item state and expose product-safe status without starting scheduled or background execution. |
| `lotus-report.reporting.batch_control_api.v1` | `POST /reports/batches/{batch_id}:pause`, `:resume`, `:cancel`, `:retry-failed`, `:recover-expired-leases` | `src/app/routers/report_batches.py`, `src/app/report_batch_orchestrator/ledger.py`, `src/app/report_batch_orchestrator/postgres_ledger.py`, `tests/integration/test_report_batch_api.py`, `tests/unit/report_batch_orchestrator/test_dispatch.py`, `tests/integration/test_postgres_report_batch_ledger.py` | Operator-safe batch control APIs over durable internal primitives. Retry only resets eligible failed items without linked report jobs, cancellation preserves already-created report jobs, and expired-lease recovery is idempotent. Workbench UI remains outside supported scope. |
| `lotus-report.reporting.batch_item_execution_bridge.v1` | Internal batch execution service | `src/app/report_batch_orchestrator/execution.py`, `src/app/report_batch_orchestrator/ledger.py`, `src/app/report_batch_orchestrator/postgres_ledger.py`, `tests/unit/report_batch_orchestrator/test_execution.py`, `tests/integration/test_postgres_report_batch_ledger.py` | Internal bridge that advances a dispatched batch item through the existing report-job, snapshot, render, and archive handoff path, then maps success or failure back to durable batch item state. This is not a public scheduler, dispatch API, gateway surface, or Workbench surface. |
| `lotus-report.reporting.batch_worker_run_bridge.v1` | Internal bounded batch worker run service | `src/app/report_batch_orchestrator/worker.py`, `src/app/report_batch_orchestrator/dispatch.py`, `src/app/report_batch_orchestrator/execution.py`, `tests/unit/report_batch_orchestrator/test_worker.py` | Internal single-batch worker run primitive that recovers expired pre-dispatch leases, dispatches eligible items under back-pressure policy, and advances waiting items through the internal execution bridge. This is not a scheduler loop, background worker process, public dispatch API, gateway surface, or Workbench surface. |
| `lotus-report.reporting.batch_run_once_api.v1` | `POST /reports/batches/{batch_id}:run-once` | `src/app/routers/report_batches.py`, `src/app/report_batch_orchestrator/service.py`, `src/app/report_batch_orchestrator/worker.py`, `tests/integration/test_report_batch_api.py`, `tests/unit/report_batch_orchestrator/test_worker.py` | Internal operator-controlled single-batch run API over the bounded worker primitive. The API can recover expired leases, dispatch eligible items under back-pressure policy, advance waiting report jobs through snapshot/render/archive reconciliation, and return product-safe counts, linked job ids, back-pressure reasons, skip reasons, and item execution outcomes. This is not a scheduler loop, background worker process, gateway surface, or Workbench surface. |
| `lotus-report.reporting.batch_runtime_pass.v1` | Internal bounded runtime pass service | `src/app/report_batch_orchestrator/runtime.py`, `src/app/report_batch_orchestrator/ledger.py`, `src/app/report_batch_orchestrator/postgres_ledger.py`, `tests/unit/report_batch_orchestrator/test_runtime.py`, `tests/integration/test_postgres_report_batch_ledger.py` | Internal runtime primitive that scans the durable ledger for runnable materialized/running batches, then advances a bounded number of batches through the existing single-batch worker. PostgreSQL proves the scan excludes paused and terminal batches. This is not a scheduler loop, daemon, gateway surface, Workbench surface, or RFC-0105 operations dashboard. |
| `lotus-report.reporting.batch_worker_process.v1` | `python -m app.report_batch_orchestrator.process`, `lotus-report-batch-worker` Docker Compose service | `src/app/report_batch_orchestrator/process.py`, `src/app/report_batch_orchestrator/runtime.py`, `docker-compose.yml`, `tests/unit/report_batch_orchestrator/test_process.py`, `tests/unit/test_docker_compose_runtime.py` | Daemonized internal background worker process that repeatedly invokes the bounded runtime pass with configured worker identity, batch limit, interval, lease policy, and back-pressure limits. This is not a schedule-cycle materializer, gateway surface, Workbench surface, or RFC-0105 operations dashboard. |
| `lotus-report.reporting.batch_scheduler_process.v1` | `python -m app.report_batch_orchestrator.scheduler_process`, `lotus-report-batch-scheduler` Docker Compose service | `src/app/report_batch_orchestrator/scheduler.py`, `src/app/report_batch_orchestrator/scheduler_process.py`, `src/app/report_batch_orchestrator/schedule.py`, `docker-compose.yml`, `tests/unit/report_batch_orchestrator/test_scheduler.py`, `tests/unit/report_batch_orchestrator/test_scheduler_process.py`, `tests/unit/test_docker_compose_runtime.py` | Daemonized internal schedule materializer that reads governed `REPORT_BATCH_SCHEDULES_JSON`, resolves explicit portfolio-list, all-active, and inline manifest schedule selectors through `lotus-core` or schedule manifest metadata, and creates durable idempotent scheduled batches using deterministic cycle identity. It does not execute items itself; the `lotus-report-batch-worker` process advances materialized batches. This is not a gateway-facing scheduler API, Workbench surface, RFC-0105 dashboard, or RFC-0106 entitlement-certified public runtime. |
| `lotus-report.reporting.batch_scheduler_admin_api.v1` | `GET /reports/batch-schedules`, `POST /reports/batch-schedules:run-due` | `src/app/routers/report_batches.py`, `src/app/report_batch_orchestrator/scheduler.py`, `tests/integration/test_report_batch_api.py`, `tests/unit/report_batch_orchestrator/test_boundary.py` | Operator-safe scheduler administration API over the governed config-backed scheduler source. The API lists configured schedules and can run one bounded scheduler materialization pass that resolves enabled schedules and creates or reuses durable idempotent batches. Schedules remain config-backed; this is not scheduler CRUD, item execution, RFC-0105 dashboarding, or RFC-0106 entitlement-certified public runtime. |
| `lotus-report.reporting.observability.traceability.v1` | Reporting request/response headers, report-to-render, report-to-archive, structured logs | `src/app/observability.py`, `src/app/clients/render_client.py`, `src/app/clients/archive_client.py`, `src/app/report_batch_orchestrator/process.py`, `src/app/report_batch_orchestrator/scheduler_process.py`, `tests/unit/test_observability.py`, `tests/unit/test_clients.py`, `tests/integration/test_api.py`, `tests/integration/test_report_job_api.py` | RFC-0105 first-wave correlation and trace propagation for report, render, archive, batch worker, and scheduler operations. Non-W3C human-readable trace ids are preserved in `X-Trace-Id` without emitting malformed `traceparent`; valid W3C trace ids emit `traceparent`. Structured logs use bounded runtime fields and do not log raw report payloads, rendered artifacts, storage keys, or client names. |
| `lotus-report.reporting.observability.metrics.v1` | `/metrics` plus `src/app/reporting_metrics.py` | `src/app/reporting_metrics.py`, `src/app/observability.py`, `src/app/reporting_operations/attention.py`, `docs/operations/reporting-observability-metrics.md`, `tests/unit/test_observability.py`, `tests/unit/reporting_operations/test_attention.py`, `tests/integration/test_api.py`, `tests/integration/test_report_batch_api.py` | RFC-0105 first-wave report metrics contract for implemented report job, snapshot, render, archive, rerender-from-snapshot, regenerate-from-upstream, failed-work replay command, batch worker, scheduler, and operations attention scan behavior. Metrics use bounded labels only and explicitly reject high-cardinality or sensitive labels such as client, portfolio, tenant, document, report job, batch, trace, correlation, storage, and raw payload fields. Dedicated broad replay dashboard metrics remain reserved until those surfaces are implemented and proven. |
| `lotus-report.aggregation.portfolio_snapshot` | `GET /aggregations/portfolios/{portfolio_id}` | `src/app/routers/aggregations.py`, `src/app/services/aggregation_service.py`, `tests/unit/test_aggregation_service.py`, `tests/integration/test_api.py` | Current reporting aggregation snapshot capability. |
| `lotus-report.integration.capabilities` | `GET /integration/capabilities` | `src/app/routers/integration.py`, `src/app/models/contracts.py`, `tests/integration/test_api.py` | Publishes current feature and workflow posture for downstream consumers. |

## Planned RFC-0104 Feature Candidates

These rows are planning markers for future surfaces, not shipped product capability. RFC-0104
first-wave closure is implementation-backed through the separate feature keys above for durable
batch materialization/status/control APIs, the internal run-once operator API, schedule-cycle
identity, dispatch/lease/back-pressure, item execution bridge, single-batch worker run, bounded
runtime-pass, worker process, scheduler process, source-backed scheduler selector primitives, and
config-backed scheduler administration APIs.

| Feature key | Planned surface | State | Notes |
| --- | --- | --- | --- |
| `lotus-report.reporting.batch_orchestration.v1` | Future full product batch orchestration capability | `planned` | Full product batch reporting orchestration is not shipped yet. Materialization/status/control APIs, the run-once operator API, gateway exposure, Workbench single-portfolio operation, and internal dispatch, item execution, single-batch worker, bounded runtime-pass, worker-process, scheduler-process, and scheduler-admin primitives are implementation-backed under separate feature keys. |
| `lotus-report.reporting.batch_scheduler.v1` | Future persisted scheduler management API for governed scheduled monthly, quarterly, semi-annual, yearly, and explicit production cycles | `planned` | Internal deterministic schedule-cycle materialization, scheduled idempotency identity, a config-backed internal scheduler process, source-backed explicit/all-active/inline-manifest scheduler selectors, and config-backed list/run-due APIs exist. No schedule CRUD API, entitlement-certified scheduler runtime, or Workbench scheduler-management surface exists yet. Continue to use configured schedules plus certified batch APIs for implemented reporting workflows. |

## Planned RFC-0105 Feature Candidates

These rows are planning markers, not shipped product capability. RFC-0105 first-wave traceability
and metrics are implementation-backed above; mutating operations and broader SLA/stuck-state
operator surfaces remain planned. Rerender-from-snapshot, regenerate-from-upstream, and failed-work replay are
implementation-backed under their specific operation keys above and are no longer represented as
planned replay work.

| Feature key | Planned surface | State | Notes |
| --- | --- | --- | --- |
| `lotus-report.reporting.operations.lookup.v1` | Broader source-backed operator lookup by batch, batch item, render, archive, document, correlation, and trace identifiers | `planned` | Report-job diagnostics are implementation-backed under `lotus-report.reporting.operations.job_diagnostics.v1`. Broader cross-identifier search remains planned until batch, render, archive, document, correlation, and trace lookup contracts are composed and certified without overclaiming replay or document retrieval semantics. |
| `lotus-report.reporting.operations.replay.v1` | Broader replay controls | `planned` | Rerender-from-snapshot, regenerate-from-upstream, failed report-job replay, and failed implementation-backed batch-item replay are implementation-backed under their specific operation keys. Broader multi-step replay, distribution, reissue, archive-housekeeping, scheduler CRUD/registry mutation, and recovery controls remain planned and require audit records, idempotency semantics, eligibility checks, and live proof. |
| `lotus-report.reporting.operations.attention_scan.v1` | `GET /reports/operations/attention` plus `src/app/reporting_operations/attention.py` | `src/app/reporting_operations/attention.py`, `src/app/routers/reporting_operations.py`, `tests/unit/reporting_operations/test_attention.py`, `tests/integration/test_report_batch_api.py` | Source-backed RFC-0105 operations attention scan for active report jobs and batch items. It emits bounded stuck-state and SLA-breach events with opaque identifiers, age, thresholds, bounded reasons, and evidence links, and excludes raw payloads, portfolio scope, tenant identifiers, correlation identifiers, and trace identifiers. |
| `lotus-report.reporting.operations.sla_dashboard.v1` | Broader SLA, retry-pressure, and dashboard signals | `planned` | Source-backed stuck-state and SLA-breach attention scan is implementation-backed under `lotus-report.reporting.operations.attention_scan.v1`. Broader replay operational dashboards, retry-pressure dashboards, and productized SLA dashboard surfaces remain planned. |

## Planned RFC-0002 Feature Candidates

No RFC-0002 feature candidates remain in planned state after RFC-0002 implementation. Future
portfolio review extensions must start as planned rows here and move to implementation-backed only
after code, tests, API contract evidence, and operational validation exist.

## Maintenance Rules

1. Update this file in the same PR that changes feature support.
2. Keep implementation-backed rows tied to concrete code and test evidence.
3. Keep planned RFC work separate from shipped behavior.
4. Keep wiki pages concise and link here instead of duplicating this table.
5. Keep `GET /integration/capabilities` aligned with this file.
